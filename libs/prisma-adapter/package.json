{
  "name": "@iron-auth/prisma-adapter",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "files": [
    "./src/index.ts"
  ],
  "scripts": {
    "lint": "eslint \"./**/*.{cjs,js,jsx,ts,tsx}\"",
    "tsc": "tsc --noEmit",
    "test": "vitest run --passWithNoTests --silent",
    "test:noisy": "vitest run --passWithNoTests",
    "clean": "rimraf .turbo && rimraf node_modules && rimraf dist"
  },
  "devDependencies": {
    "@iron-auth/core": "workspace:^",
    "@tsconfig/strictest": "^1.0.2",
    "@vitejs/plugin-react": "^2.1.0",
    "eslint": "^8.33.0",
    "eslint-config-ixn": "^1.3.1",
    "prettier": "^2.8.3",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.5",
    "vite": "^3.1.6",
    "vitest": "^0.24.0"
  },
  "peerDependencies": {
    "@prisma/client": "^4.3.0"
  }
}
